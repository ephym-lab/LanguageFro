# API Configuration
# Point this to your backend API server running locally or remotely
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# The API base path (usually /api for most backends)
# Update if your backend uses a different endpoint structure
NEXT_PUBLIC_API_BASE_PATH=/api
